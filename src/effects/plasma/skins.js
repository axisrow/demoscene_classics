// Named visual skins for plasma. In API v3 a skin only carries *visual* choices
// (palette, motion tempo, render quality); algorithmic identity lives in config.
// 'classic' is the zero-overlay skin: effect defaults stand unchanged.
export const PLASMA_SKINS = Object.freeze({
  classic: Object.freeze({})
});
